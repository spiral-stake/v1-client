const BtnGreen = ({ text }: { text: string }) => {
  return (
    <div className="bg-white w-fit bg-opacity-[5%] p-[8px] text-[14px] font-normal rounded-[8px]">{text}</div>
  );
};

export default BtnGreen;
